[project]
name = "tabexplain"
version = "0.1.0"
description = "FastAPI backend for TabExplain - CSV data analysis and visualization"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "backend.main:app"

